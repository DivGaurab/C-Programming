//Write a program to find the largest and smallest number in a list of N
//number using DMA.
#include<stdio.h>
#include<stdlib.h>
int main(){
	int *fp, i, j;
	fp = (int*)malloc(4, sizeof(int));
	
}
